"""
clear():>to clear array
extend():>to spread one array inside other
x=count(item):>returns count of item so we need to store in new variable
x=index(item):>returns index of item so we need to store in new variable
"""
